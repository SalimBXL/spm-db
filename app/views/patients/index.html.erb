<h1>Patients</h1>

<p>
    <a href="<%= new_patient_path %>" class="btn btn-secondary" role="button"
        data-toggle="tooltip" title="New Patient">
        <span class="material-icons">person_add</span>
    </a>
</p>

<div id="patients">
    <%= render 'patients' %>
</div>